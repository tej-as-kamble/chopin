const HelpSearch = () => {
  return (
    <input
      className="help-search"
      type="text"
      placeholder="Search for help with orders, payments, account..."
    />
  );
};

export default HelpSearch;
