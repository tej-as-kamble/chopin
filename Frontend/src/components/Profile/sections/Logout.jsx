const Logout = () => {
  return (
    <div className="profile-section-card">
      <h2>Logout</h2>
      <button className="profile-primary-btn">Logout</button>
    </div>
  );
};

export default Logout;
